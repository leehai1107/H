<UserControl x:Class="ERP.Desktop.Views.PurchaseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:ERP.Desktop.Views"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800">
    <Grid Background="#F4F6F8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Margin="24,24,24,12" Background="White" CornerRadius="12" Padding="20">
            <StackPanel>
                <TextBlock FontSize="26" FontWeight="SemiBold" Text="Purchase"/>
                <TextBlock Margin="0,6,0,0" Foreground="#667085" Text="Danh sách phiếu mua và tìm kiếm nhanh."/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1" Margin="24,0,24,12" Background="White" CornerRadius="12" Padding="16">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
                    <Border Margin="12,0,0,0" Background="#EEF2FF" CornerRadius="10" Padding="12,8">
                        <StackPanel>
                            <TextBlock FontSize="20" FontWeight="Bold" Text="{Binding TotalOrders}"/>
                            <TextBlock Foreground="#667085" Text="Tổng"/>
                        </StackPanel>
                    </Border>

                    <Border Margin="12,0,0,0" Background="#ECFDF3" CornerRadius="10" Padding="12,8">
                        <StackPanel>
                            <TextBlock FontSize="20" FontWeight="Bold" Text="{Binding ActiveOrders}"/>
                            <TextBlock Foreground="#667085" Text="Đang hoạt động"/>
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBox Width="320"
                             Height="36"
                             VerticalContentAlignment="Center"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             ToolTip="Tìm theo mã, nhà cung cấp hoặc trạng thái"/>
                </StackPanel>
            </DockPanel>
        </Border>

        <Border Grid.Row="2" Margin="24,0,24,24" Background="White" CornerRadius="12" Padding="12">
            <DataGrid ItemsSource="{Binding PurchaseView}"
                      SelectedItem="{Binding SelectedOrder}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      Background="White"
                      BorderThickness="0">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Mã" Binding="{Binding Code}" Width="120"/>
                    <DataGridTextColumn Header="Nhà cung cấp" Binding="{Binding SupplierName}" Width="*"/>
                    <DataGridTextColumn Header="Ngày mua" Binding="{Binding OrderDate, StringFormat=d}" Width="120"/>
                    <DataGridTextColumn Header="Số tiền" Binding="{Binding Amount, StringFormat=N0}" Width="140"/>
                    <DataGridTextColumn Header="Trạng thái" Binding="{Binding Status}" Width="140"/>
                    <DataGridCheckBoxColumn Header="Hoạt động" Binding="{Binding IsActive}" Width="100"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
    </Grid>
</UserControl>