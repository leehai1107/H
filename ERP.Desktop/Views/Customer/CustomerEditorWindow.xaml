<mah:MetroWindow x:Class="ERP.Desktop.Views.Customer.CustomerEditorWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
                 Title="Customer"
                 Width="520"
                 Height="460"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="NoResize"
                 WindowStyle="None"
                 ShowInTaskbar="False">
    <Border Background="White" Padding="20">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel>
                <TextBlock FontSize="22" FontWeight="SemiBold" Text="Customer"/>
                <TextBlock Margin="0,6,0,0" Foreground="#6B7280" Text="Nhập thông tin khách hàng"/>
            </StackPanel>

            <Grid Grid.Row="1" Margin="0,18,0,18">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="140"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Mã"/>
                <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,12" Height="32" Text="{Binding Code, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Tên"/>
                <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,12" Height="32" Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Điện thoại"/>
                <TextBox Grid.Row="2" Grid.Column="1" Margin="0,0,0,12" Height="32" Text="{Binding Phone, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Email"/>
                <TextBox Grid.Row="3" Grid.Column="1" Margin="0,0,0,12" Height="32" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>

                <TextBlock Grid.Row="4" Grid.Column="0" Margin="0,0,12,12" VerticalAlignment="Center" Text="Địa chỉ"/>
                <TextBox Grid.Row="4" Grid.Column="1" Margin="0,0,0,12" Height="32" Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}"/>

                <CheckBox Grid.Row="5" Grid.Column="1" Content="Đang hoạt động" IsChecked="{Binding IsActive}"/>
            </Grid>

            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Width="100" Height="34" Margin="0,0,8,0" Content="Hủy" Command="{Binding CancelCommand}"/>
                <Button Width="100" Height="34" Content="Lưu" Command="{Binding SaveCommand}"/>
            </StackPanel>
        </Grid>
    </Border>
</mah:MetroWindow>