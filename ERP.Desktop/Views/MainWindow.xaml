<mah:MetroWindow x:Class="ERP.Desktop.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ERP.Desktop"
        xmlns:mah="clr-namespace:MahApps.Metro.Controls;assembly=MahApps.Metro"
        xmlns:controls="clr-namespace:ERP.Desktop.Controls.Layout"
        mc:Ignorable="d"
        Title="ERP Metal Fabrication" Height="450" Width="800">
    <Grid>

        <Grid.RowDefinitions>

            <RowDefinition Height="55"/>

            <RowDefinition Height="*"/>

            <RowDefinition Height="28"/>

        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>

            <ColumnDefinition Width="220"/>

            <ColumnDefinition Width="*"/>

        </Grid.ColumnDefinitions>
        
        <controls:HeaderControl

            Grid.Row="0"

            Grid.ColumnSpan="2"/>
        <controls:SidebarControl

            Grid.Row="1"

            Grid.Column="0"/>
        <Border
    Grid.Row="1"
    Grid.Column="1"
    Background="#F4F6F8">

            <ContentControl
    Grid.Row="1"
    Grid.Column="1"
    Content="{Binding CurrentView}" />

        </Border>

        <controls:StatusBarControl
    Grid.Row="2"
    Grid.ColumnSpan="2"/>

    </Grid>
</mah:MetroWindow>
